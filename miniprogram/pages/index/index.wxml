
<scroll-view class="main" scroll-y="{{true}}">
  <view class="slogen-wrap" >
    <view class="slogen"><view class="special">demo示例</view></view>
    <view class="intro">
      <!-- 微信小程序 -->
    </view>
  </view>

  <pull-down-list
    wx:for="{{list}}"
    name="{{item.name}}"
    tag="{{item.tag}}"
    root="{{root}}"
    wx:key="name"
    childArr="{{item.childArr}}"
    bind:click="clickHandle"
  />
</scroll-view>